name: Build iOS Tweak (HTML)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Dependencies
      run: |
        brew install make
        brew install ldid

    - name: Upgrade GNU Make (Fix parallel build warning)
      run: |
        brew upgrade make || brew install make
        echo "PATH=$(brew --prefix make)/libexec/gnubin:$PATH" >> $GITHUB_ENV

    - name: Setup Theos
      run: |
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV
        echo "$HOME/theos/bin:$PATH" >> $GITHUB_PATH

    - name: Setup iOS SDK
      run: |
        rm -rf $HOME/theos/sdks
        git clone --depth 1 https://github.com/theos/sdks.git $HOME/theos/sdks

    - name: Build Tweak
      run: |
        export THEOS=$HOME/theos
        export ARCHS=arm64
        export TARGET=iphone:clang:latest:16.5
        make package FINALPACKAGE=1

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: NexoraHTML-Tweak
        path: packages/*.deb
        if-no-files-found: warn
